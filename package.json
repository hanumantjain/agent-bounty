{
  "name": "agentbounty",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "agent:start": "npm run start --prefix agent",
    "agent:start:blocked": "npm run start:blocked --prefix agent",
    "agent:create-bounty": "npm run create-bounty --prefix agent",
    "contracts:compile": "npm run compile --prefix contracts",
    "contracts:deploy": "npm run deploy --prefix contracts"
  }
}
