# Testing

Automated tests cover the deterministic logic and the backend's payment-rejection paths.
The full paid/on-chain flows are exercised against real Hedera + Sepolia testnet — re-running
those on every test invocation would cost real (testnet) funds and gas on every run, so those
behaviors are verified live and the evidence is recorded here instead of re-asserted automatically.

Run automated tests: `npm test` from the repo root (runs `agent/` and `backend/` test suites).

## Guide's test checklist

| # | Scenario | Coverage |
|---|---|---|
| 1 | Authorized agent passes | Live: `researcher` identity completes a real bounty end-to-end (Phase 5 commit `d5e04a8`) |
| 2 | Unauthorized agent fails | Live: with the data price temporarily spiked above an identity's own resolved spending limit, that identity never claims and never pays, bounty left fully untouched (`Open`, zero agent) — confirmed on real testnet (see the price-spike attack demo, row 4, and the usage-based-metering row below for the current mechanism: post-metering, an identity is blocked when its limit can't even afford `minFirst=1`, not by a fixed identity-vs-identity split) |
| 3 | Price below limit pays | Automated: `agent/test/decide.test.js` ("price below limit is allowed"); Live: `researcher` run above |
| 4 | Price above limit does not attempt payment | Automated: `agent/test/decide.test.js` ("price above limit is blocked"); Live: attack demo, Phase 8 — price spiked to 2 HBAR against `researcher`'s 1 HBAR limit, blocked before any claim, zero new transactions confirmed via mirror node |
| 5 | Unpaid API returns 402 | Automated: `backend/test/data.test.js` ("unpaid request returns 402 with payment requirements") |
| 6 | Invalid payment is rejected | Automated: `backend/test/data.test.js` (malformed `X-PAYMENT` header, and a well-formed-but-fake transaction rejected live by the real Blocky402 facilitator) |
| 7 | Live Graph failure fails safely | By construction: `backend/lib/graph.js` throws on any non-2xx/GraphQL-error response, and `backend/routes/data.js` converts that to a `502` — never falls back to stub/fake data (verified manually in Phase 2 by breaking `GRAPH_API_KEY`) |
| 8 | Correct answer pays | Live: independent check matched a genuine agent answer, a human approved via `POST /api/bounty/:taskId/decide` → status `Paid`, reward transferred (Phase 11) |
| 9 | Wrong answer does not pay | Live: a deliberately fabricated answer claimed + submitted directly → independent check flagged the mismatch (`matches: false`) → human rejected → status `Rejected`, reward untouched (Phase 11) |
| — | Exclusive claim (not in the guide's original list, added with the claim step) | Live: claiming the same bounty twice in immediate succession — the second `claimBounty` call reverts with `"not open"`, confirmed on real testnet (`contracts/scripts/testFlow.js`) |
| — | Task-type capability gate (not in the guide's original list, added with multi-task-type support) | Live: a bounty created with an unrecognized `taskType` (bypassing `POST /api/bounty/create`'s validation, directly via the contract) is discovered but never claimed — the agent stops at the capability check before any price probe, ENS lookup, or transaction; confirmed zero new transactions via mirror node. A second real task type (`deposit-anomaly`, querying the same subgraph's `deposits` entity) was run end-to-end and correctly flagged a genuine ~$40.1M outlier deposit as `SUSPICIOUS`, matched by the independent re-check, and approved |
| — | Multi-bounty discovery and selection (not in the guide's original list, added with multi-bounty support) | Live: with 4 open bounties simultaneously (one unsupported task type, two supported/affordable, one deliberately unaffordable), a single agent run correctly skipped the unsupported one, claimed and completed the first eligible one, and left the other two untouched (`Open`, no agent) — confirmed via `GET /api/bounty/list`. A follow-up run with the data price spiked above the identity's limit skipped every remaining candidate (two unsupported-task, one over-budget) and terminated in `no-eligible-bounty` with zero new transactions, confirmed via mirror node |
| — | Activate-and-race with auto-payout (not in the guide's original list, added with the race feature) | Live, through the real browser UI: clicking Activate Bounty on an eligible bounty kicked off both identities concurrently — `intern` correctly `skip-blocked` (policy still enforced mid-race), `researcher` claimed, paid, analyzed, submitted, and was auto-verified and paid with **zero human interaction**, confirmed `Paid` via `GET /api/bounty/:taskId`. A separately-fabricated wrong answer run through the same auto-verify path (`verifyBounty`) was correctly caught and rejected, reward untouched. Surfaced and fixed a real bug along the way: Hashio's gas estimation under-shoots for `releaseReward` (it moves native HBAR), causing a silent on-chain revert that `waitForTransactionReceipt` doesn't throw on by default — every contract write now goes through `writeAndConfirm`, which pins an explicit gas limit and checks `receipt.status` itself |
| — | Manager identity (not in the guide's original list) | `agentbounty.eth`'s Hedera account — the same one that creates bounties and deployed the contract — is the deployed contract's on-chain `verifier`, confirmed by reading the contract's `verifier()` getter directly and comparing it to that account's address. `researcher`/`intern` each have their own separate, independently-funded accounts, confirmed by the race correctly enforcing each one's distinct ENS spending policy |
| — | Additional task categories reuse the same subgraph, no new Graph node (not in the guide's original list) | Live: confirmed via direct schema introspection that the existing subgraph already exposes `Borrow`/`Repay`/`Liquidate` entities in the identical shape used by the original two categories. Added `borrow-anomaly`, `repay-anomaly`, `liquidation-anomaly` to the task registry with zero contract, backend-route, or frontend changes; ran all three live and each correctly flagged a genuine large real-world event ($12.7M borrow, a repayment, an $11.5M liquidation) as `SUSPICIOUS`, auto-verified and paid by `agentbounty.eth`. The Post-a-Bounty dropdown picked up all five categories automatically, confirming the task-type registry is the actual single source of truth, not just in name |
| — | Cross-protocol composition — same query, three live Messari lending subgraphs (not in the guide's original list) | Live: verified each of Aave V3 and Compound III independently (direct schema introspection, both actively syncing) before wiring them in alongside the existing Morpho Aave V3 subgraph. Ran a real bounty end-to-end and confirmed the paid response's `items` were tagged with all three protocol names (`morpho-aave-v3`, `aave-v3`, `compound-v3`) from one merged query, `largest.protocol` correctly identified which protocol held the flagged amount, and the independent verifier's re-check queried the same three protocols and matched |
| — | Enhanced Access Control delegation to a genuinely separate account (not in the guide's original list) | Live: `agent/scripts/provePolicyDelegation.js` — the admin (`0x375552...4f33`) granted a freshly-generated, previously-unused Sepolia account (`0x1d21ec...cef0`) a role scoped to just `agent.spending.limit`. That operator account then signed its own `setText` transaction (confirmed via the receipt's `from` field matching the operator, not the admin) and succeeded. The same operator then tried to set a different text key ("avatar") and tried to call `authorizeTextRoles` itself — both reverted on-chain as expected, since it was only ever granted the one key-scoped role. Confirmed the app's live behavior was unaffected afterward (`researcher`'s spending limit still resolves to its current value) |
| — | Usage-based metering — price scales with volume, agent budgets against it (not in the guide's original list) | Live: `GET /api/data/pricing` returns a real `pricePerItemTinybars`; probing `first=5` vs `first=20` for the same task priced exactly 4x apart, confirming `price = pricePerItemTinybars × records × protocols` genuinely holds, not a flat fee. Each agent identity reads this before probing and computes the deepest sample its own ENS-resolved spending limit affords — a live 3-way race with `intern` (1 HBAR), `researcher` (5 HBAR), `director` (10 HBAR) produced three different real purchases (`firstPerProtocol` 2, 11, and 20 respectively) at proportionally correct prices (0.9, 4.95, 9 HBAR), confirmed via the SSE step log and matching mirror-node settlements. The independent verifier's re-check (`checkAnswer`) replicates the exact `firstPerProtocol` the agent actually paid for (embedded in its submitted answer) rather than a fixed default, so a budget-constrained answer isn't unfairly rejected against a larger window — confirmed the `intern` winner above was still correctly auto-verified and paid despite its smaller sample |
| — | Third independent worker identity, "director" (not in the guide's original list) | Live: `director.agentbounty.eth` set up on the existing self-deployed Sepolia registry (real `register`/resolver-deploy/`authorizeTextRoles`/`setText` transactions), with its own dedicated, correctly EVM-aliased Hedera account (confirmed via mirror node: `evm_address` exactly matches the address derived from its raw private key). Wired into the claim race alongside `researcher`/`intern`; a genuine 3-way on-chain race showed all three attempt `claimBounty` concurrently, exactly one lands and the other two get real `skip-claim-failed` reverts, confirmed across separate runs that the winner is not fixed (`director` won one run, `researcher` a different run, `intern` a third) |
| — | Verifiable payment audit trail on HCS (not in the guide's original list) | Live: created a real HCS topic (`0.0.10462976`) via `agent/scripts/setupHcsAuditTopic.js`; every x402 settlement in `backend/middleware/requirePayment.js` now submits a `TopicMessageSubmitTransaction` recording the resource, asset, amount, payer, payTo, and settlement tx id, returning `{topicId, sequenceNumber}`, which travels into the agent's on-chain answer. Independently fetched a logged message straight from the mirror node (`GET /api/v1/topics/{topicId}/messages/{sequenceNumber}`, bypassing the app entirely) and confirmed its decoded contents matched the real payment exactly (900,000,000 tinybars, correct payer/payTo, correct settlement tx id). Surfaced and fixed a real bug along the way: the slightly larger `answer` payload (now carrying the audit record) pushed `submitAnswer` past the fixed `300_000` gas limit in `writeAndConfirm`, causing `INSUFFICIENT_GAS` reverts — raised to `600_000` across all four contract writes |
| — | HTS token as a second real settlement asset (not in the guide's original list) | Live: before building, empirically confirmed the Blocky402 facilitator is asset-agnostic — created a throwaway HTS token and got a real `200`/`isValid:true` from `/verify` and a real `success:true` from `/settle` for it, independently confirmed via the mirror node as a genuine `token_transfers` entry. Built on that: created a permanent token, "AgentBounty Data Credit" (`0.0.10464008`, `ADC`), and gave `intern`/`researcher`/`director` deliberately different starting balances (0 after a deliberate drain / 500 / 2000). `backend/middleware/requirePayment.js` generalized to offer both HBAR and ADC in the same 402 challenge; each agent checks its own real ADC balance (mirror node, not a declared policy) before choosing. Verified both branches live: `director` paid a real 60.00 ADC settlement, confirmed via mirror node as a `token_transfers` entry (not HBAR); `intern`, with 0 ADC, correctly fell back and paid 0.9 HBAR instead, confirmed zero token transfer for that settlement |

## What's automated vs. what's live-verified

**Automated** (`npm test`, no network cost, safe to re-run anytime):
- `agent/test/decide.test.js` — spending-limit affordability logic, including exact-boundary cases.
- `agent/test/analyze.test.js` — anomaly-analysis verdict logic (shared by both task types), including empty-list and exact-threshold edge cases.
- `backend/test/data.test.js` — the paid endpoint's rejection paths (402 on no payment, rejection of a malformed header, and a live call to the real Blocky402 facilitator confirming it rejects a fabricated transaction).

**Live-verified, not re-run automatically** (would cost real testnet HBAR/gas or require live ENS/Graph state per run):
- Full agent runs (discovery → decide → claim → payment → data → analysis → submission) for `researcher`, `intern`, and `director`.
- The exclusive-claim guarantee (double `claimBounty` reverts).
- The human-review check/decide endpoints, both the approve and reject paths, against the real `BountyEscrow` contract.
- The price-spike attack scenario.

To re-verify any of these live paths yourself: `npm run agent:create-bounty`, then `npm run agent:start` (or `agent:start:intern`). Once `Submitted`, either use the **Bounty Details** dashboard screen, or from `agent/`: `npm run verify -- <taskId>` (runs the check and acts on its own verdict immediately, for scripted testing).
