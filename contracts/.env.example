HEDERA_JSON_RPC_URL=https://testnet.hashio.io/api
DEPLOYER_PRIVATE_KEY=
VERIFIER_ADDRESS=
# The ADC HTS token's Hedera ID (see agent/.env.example's DATA_CREDIT_TOKEN_ID) — passed to the
# constructor so the contract knows which token it's allowed to hold/pay out as an alternate
# reward asset. Converted to its EVM "long-zero" address form by deploy.js.
DATA_CREDIT_TOKEN_ID=
